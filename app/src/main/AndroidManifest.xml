<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>


    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:name=".webservices.AppController"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:allowBackup"
        android:theme="@style/Theme.GrievanceMS">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GrievanceMS.NoActionBar">
        </activity>
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.GrievanceMS.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.CreateGrievanceActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".ui.OfflineSurveysListActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.OfflineDetailsActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.TodayGrievanceActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.TodayGrievanceDetailsActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".ui.home.ViewImageActivity"
            android:theme="@style/Theme.GrievanceMS.NoActionBar"
            android:windowSoftInputMode="stateHidden"/>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>